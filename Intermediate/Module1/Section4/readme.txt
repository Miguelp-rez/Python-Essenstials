Create a virtual environment.
python -m venv venv

Move to the 'venv' folder
cd venv/

Activate a virtual environment
Scripts\activate.bat

Install pygame
pip install --upgrade pip
pip install pygame

Move the 'how_to_pygame.py' file to the local 'venv' folder, then run the script

Deactivate when finished
deactivate
